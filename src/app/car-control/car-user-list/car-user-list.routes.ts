import { Routes } from '@angular/router';
import { CarUserListComponent } from './car-user-list.component';

export const CAR_USER_ROUTES: Routes = [
  { path: '', component: CarUserListComponent },
];
